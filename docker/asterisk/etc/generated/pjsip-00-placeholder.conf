; Keep me. pjsip.conf ends with `#include generated/pjsip-*.conf`, and a
; wildcard include matching zero files makes Asterisk reject the entire
; pjsip.conf — leaving no SIP transport at all, with no obvious error.
