; Static dialplan. Device extensions and the echo test are generated from the
; database into generated/dialplan-*.conf.
[general]
static = yes
writeprotect = no

[default]
exten => _X.,1,NoOp(twocans: call arrived outside any device context)
 same => n,Hangup()

#include generated/dialplan-*.conf
